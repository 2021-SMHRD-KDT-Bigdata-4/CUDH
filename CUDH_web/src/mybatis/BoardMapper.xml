<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.BoardMapper">
  
    <select id="loginMember" resultType="VO">
       select * from member where member_id= #{member_id} and member_pw=#{member_pw}
    </select>
    
   	<select id="boardList" resultType="VO">
    select board_num,board_title,board_content,board_grade,member_num from board
    </select>
</mapper>