<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mybatis.BoardMapper">
  
    <select id="loginMember" resultType="VO">
       	select * from member where m_id= #{m_id} and m_pw=#{m_pw}
    </select>
    
   	<select id="boardList" resultType="VO">
    	select b_idx, b_category ,b_area , b_title, b_writer, b_contents, b_views  from board
    </select>
    <select id="memberBoardContent" resultType="VO">
    	select b_idx, b_category ,b_area , b_title, b_writer, b_contents, b_views from board where b_idx=#{b_idx}
    </select>
</mapper>